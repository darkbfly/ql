# js 模块文档

> **导航**: [ql 项目根目录](../CLAUDE.md) > js 模块

## 模块概述

`js` 模块包含独立的 JavaScript 自动化脚本，主要用于各类应用的签到和任务自动化。

## 模块结构

```
js/
├── bsd.js        # 波司登小程序脚本
├── 媓钻.js        # 媓钻相关脚本
└── 悦秀会.js      # 悦秀会相关脚本
```

## 关键文件

### `bsd.js`

**功能**：波司登小程序自动化脚本。

**主要特性**：
- 使用 Env 环境管理
- 支持多账号 Cookie 处理
- 集成通知服务
- 支持 Node.js 和浏览器环境

**代码结构**：
```javascript
const $ = new Env("波司登小程序");
let ckName = `bosideng`;
let userCookie = checkEnv(
    ($.isNode() ? process.env[ckName] : $.getdata(ckName)) || ""
);
const notify = $.isNode() ? require("./sendNotify") : "";
```

**功能模块**：
- Cookie 解析和处理
- 用户任务执行（`records()` 函数）
- 通知发送

### `媓钻.js`

**功能**：媓钻相关自动化脚本。

### `悦秀会.js`

**功能**：悦秀会相关自动化脚本。

## 接口说明

### 通用接口模式

所有脚本遵循类似的接口模式：

```javascript
// 1. 初始化环境
const $ = new Env("脚本名称");
let ckName = `cookie_name`;

// 2. 获取 Cookie
let userCookie = checkEnv(
    ($.isNode() ? process.env[ckName] : $.getdata(ckName)) || ""
);

// 3. 加载通知服务
const notify = $.isNode() ? require("./sendNotify") : "";

// 4. 主执行逻辑
!(async () => {
    // 脚本逻辑
})();
```

## 使用场景

1. **小程序自动化**：微信小程序签到和任务自动化
2. **品牌活动**：参与各类品牌活动和任务
3. **多账号管理**：支持多账号 Cookie 批量处理

## 依赖关系

### 外部依赖

- **Env 类**：环境管理类（可能来自青龙框架或自定义）
- **sendNotify**：通知服务模块（Node.js 环境）
- **HTTP 请求库**：可能使用 `got`、`request` 或 `axios`

### 被依赖

- 可能被青龙面板调用执行
- 可能被其他自动化工具集成

## 配置说明

### 环境变量

脚本通过环境变量获取 Cookie：
- 环境变量名称通常与脚本相关（如 `bosideng`）
- 支持多账号，使用特定分隔符（如 `#`、`@` 或换行）

### 运行环境

1. **Node.js 环境**：
   - 需要安装相应的依赖包
   - 通过 `process.env` 获取环境变量

2. **浏览器环境**：
   - 使用 `$.getdata()` 获取本地存储的数据
   - 可能需要浏览器扩展支持

## 注意事项

1. **Cookie 格式**：不同脚本可能使用不同的 Cookie 格式和分隔符
2. **环境兼容**：脚本需要兼容 Node.js 和浏览器两种环境
3. **通知服务**：Node.js 环境需要 `sendNotify` 模块支持
4. **错误处理**：建议添加完善的错误处理和日志记录

## 脚本特点

1. **统一接口**：所有脚本遵循相似的接口模式
2. **多账号支持**：支持批量处理多个账号
3. **通知集成**：集成通知服务，及时反馈执行结果
4. **环境适配**：兼容 Node.js 和浏览器环境

## 下一步建议

1. **代码规范**：统一代码风格和接口规范
2. **错误处理**：添加完善的异常处理机制
3. **日志记录**：统一日志记录格式和级别
4. **测试覆盖**：添加单元测试和集成测试
5. **文档完善**：为每个脚本添加详细的使用说明

---

*本文档由 `/zcf/init-project` 命令自动生成*

